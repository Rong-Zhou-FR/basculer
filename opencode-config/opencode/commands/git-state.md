---
description: verify repo status : dangling commits ? unmerged feat ?
agent: copilot
---
Are there dangling commits/unmerged feature branches in the current repo ? Do they contain any useful features not present in main ? Would merging them cause regression ?


Follow conventions in AGENTS.md.
