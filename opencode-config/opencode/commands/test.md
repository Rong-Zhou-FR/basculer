---
description: Add or run tests
agent: tester
---

Work on tests for specified code or feature.

Input:
- Code to test (file, function, or feature)
- Or "run existing" to run test suite

Process:
1. Ask user to clarify input if unclear
2. Write new tests (only *.test.*, *.spec.*, tests/**), OR
3. Run existing tests
4. Show results, focus on failures