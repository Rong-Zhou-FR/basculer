---
description: update tracking after fix
agent: copilot
---

Great work. 

1. commit and push changes, if not yet done
  - if current repo is not git repo, check sub-folders. some workspaces have each module initialised as an individual git repo
  - you mention the relevant github issue(s) (#n) in your commit message
    - if a Github issue has been solved, close with a closing comment
    - If no github issue has been created on the current workcycle: Create it to document:
      - the problem identified
      - the fixes proposed
      - the implementation
      - test results
    - and close it with a closing comment
2. Update AGENTS.md, README.md and readthedocs files to reflect latest project state
