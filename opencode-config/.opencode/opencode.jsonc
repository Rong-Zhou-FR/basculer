{
	"$schema": "https://opencode.ai/config.json",
	// Must be loaded AFTER opencode-browser-plugin to intercept its tools
	"plugin": ["./plugins/browser-safety.ts"]
	// Add MCP servers, tools, plugins here
}
