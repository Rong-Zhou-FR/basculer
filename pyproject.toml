[project]
name = "basculer"
version = "0.1.0"
description = "Collection of productivity tweaks for Debian-based Linux"
requires-python = ">=3.12"

[dependency-groups]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
