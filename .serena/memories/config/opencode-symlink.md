# OpenCode Config Location

The `~/.config/opencode` folder is a symbolic link to `./opencode` in the basculer project directory.

When editing opencode configs, use paths relative to basculer project, e.g.:
- Agent configs: `basculer/opencode/agents/githubber.md`
- Not: `~/.config/opencode/agents/githubber.md`